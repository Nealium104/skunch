---
import Layout from "../layouts/Layout.astro";
import { client } from "../../netlify/functions/contentful";
import { documentToHtmlString } from "@contentful/rich-text-html-renderer";
import type { EntryFieldTypes } from "contentful";

// interface ConcertDate {
//   contentTypeId: "concertDate",
//   fields: {
//     Venue: EntryFieldTypes.Text
//     Images: EntryFieldTypes.AssetLink,
//     Date: EntryFieldTypes.Date,
//   }
// }

client.getEntry('<concertDate>').then(function (entry) {
  console.log(entry)

  console.log(entry.fields.venue)
})

---
<Layout>
<button id="fetchButton">Click</button>

<body>
  <h1>This is the skunchbucket</h1>
    <!-- {entries.items.map(item => (
        <article>
            <h1>This is a test</h1>
            <h2>{item.fields.Venue}</h2>
            <p>{item.fields.Date}</p>
            <img src={item.fields.file.url} alt="image"/>
        </article>
    ))} -->
</body>
<script src="https://cdn.jsdelivr.net/npm/contentful@latest/dist/contentful.browser.min.js"></script>
</Layout>.